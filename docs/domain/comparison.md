# Comparison

A Comparison is a first-class analytical result relating two or more subjects.

Possible subjects:

- runs;
- artifacts;
- configurations;
- datasets.

Comparison is computed by the Analysis Plane, represented by Semantic Layer contracts, and retrieved through application/query services exposed by the Platform API.

The UI and MCP must not independently redefine comparison formulas.
